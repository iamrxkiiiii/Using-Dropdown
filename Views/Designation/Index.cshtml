@model IEnumerable<DesignationEntity>

<div class="col-12">
    <h1>View Designation</h1>

    <p>
        <a asp-action="Create" class="btn btn-primary text-end">Create New</a>
    </p>

    @if (Model != null && Model.Any())
    {
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.First().DesignationId)</th>
                    <th>@Html.DisplayNameFor(model => model.First().DesignationName)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(model => item.DesignationId)</td>
                        <td>@Html.DisplayFor(model => item.DesignationName)</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.DesignationId" class="btn btn-warning">Edit</a>
                            <a asp-action="Delete" asp-route-id="@item.DesignationId" class="btn btn-danger ms-2"
                               onclick="return confirm('Are you sure you want to delete this designation?');">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-warning">No designation records found.</div>
    }
</div>
